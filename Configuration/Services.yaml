services:
  _defaults:
    autowire: true
    autoconfigure: true
    public: false

  Imhlab\TtnewsLinkConverter\:
    resource: '../Classes/*'
    exclude: '../Classes/Domain/Dto/*'

  # Register Imhlab\TtnewsLinkConverter\Command\ConvertLinksCommand here with a
  # 'console.command' tag once it exists - see vendor/imhlab/film/Configuration/Services.yaml
  # for the exact house pattern (command name, schedulable, description).
